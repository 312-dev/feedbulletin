# Forum Reader — local dev secrets.
# Per global rule: secrets live in 1Password MCP vault, injected at runtime
# via direnv. Nothing here is plaintext; .envrc itself is safe to commit
# because it only carries op:// references.

# Anthropic API key — drives the site-profile learner (scrape::handle_sample)
# and the debugger-mode refiner. Reusing an existing local-scope key.
export ANTHROPIC_API_KEY="$(op read 'op://MCP/hgdnixfuvu5aqjo3u3sn3uoycm/password')"
